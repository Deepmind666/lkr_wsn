# 正式论文差距评估（截至 2025-11-03）

## 总体结论
- 整体准备度约 70–75%，主要差距集中在图表与文稿对齐、少量模块一致性与复现流程固化。
- 协议实现与核心模型基本完备，统计显著性与多重比较已具备；图 3（骨架与网关选择）和若干说明性文本仍需补齐。

## 关键缺口与影响等级（按优先级）
1) 图 3 流程图缺失（高）
   - 现状：`results/publication_figures/aeris_figure_3_skeleton_gateway.mmd` 仅有标题注释，未含 Mermaid 流程。
   - 影响：直接影响方法章节与结果图集的一致性与可读性。
   - 修复：补全骨架与网关选择的流程图（Mermaid 或由 `scripts/plot_paper_figures.py` 的流程函数生成替代）。
   - 工期：0.5 天。

2) 真实信道模型文件一致性（中-高）
   - 现状：`RealisticChannelModel` 被 `realistic_leach_protocol.py` 与 `honest_analysis.py` 引用，但工作区树未显示 `src/realistic_channel_model.py`；需确认路径与接口。
   - 影响：方法复现与与 AERIS 的一致性论证。
   - 修复：核对文件存在性与接口签名，统一在 AERIS 初始化与链路度量中调用路径。
   - 工期：0.5–1 天。

3) 能耗模型参数校准与披露（中）
   - 现状：`ImprovedEnergyModel` 已实现且有 `fix_energy_model.py` 提及功放系数问题；论文需补充参数来源、校准流程与敏感性说明。
   - 影响：可信性与可复现实证。
   - 修复：在方法章节新增参数表与校准依据（如 CC2420/CC2650/ESP32 LoRa），并在附录给出敏感性曲线或对比。
   - 工期：0.5 天。

4) 统计效应量脚本缺失（中）
   - 现状：`compute_effect_sizes.py` 不存在；`run_export.py` 可改为使用 `plot_paper_figures.py` 中的非参数效应量函数或补齐脚本。
   - 影响：结果解释力度与对比清晰度。
   - 修复：在导出流程统一效应量计算来源，并在结果章节标注效应量。
   - 工期：0.5 天。

5) 方法接口签名摘要与 I/O 对齐（中）
   - 现状：`aeris_protocol.py`、`cas_selector.py`、`gateway_selector.py`、`skeleton_selector.py`、`fuzzy_logic_system.py` 等未形成统一的 I/O 摘要表。
   - 影响：方法章节的可读性与审稿人复核效率。
   - 修复：整理输入/输出字段与关键函数签名，纳入附录或方法表格。
   - 工期：1 天。

6) 复现与打包一致性（中）
   - 现状：`run_export.py`、`run_stats_multitest.py` 的输出与 `docs/Figure_*` 引用需统一；`results/plots_curated` 需补齐完整图集。
   - 影响：交付包一致性与复现实操难度。
   - 修复：固化输出路径、命名规范与校验脚本，更新 `RUN_GUIDE.md`。
   - 工期：0.5 天。

7) 基线参数对齐与公平性说明（低-中）
   - 现状：LEACH/PEGASIS/HEED 参数需与 AERIS 的环境与能耗设定对齐；`fairness_metrics.py` 的使用需明确在结果解释中。
   - 影响：对比公平性与结论稳健性。
   - 修复：统一随机种子与参数表，并在方法或附录说明公平性控制策略（Jain 指数与 CH 轮换惩罚）。
   - 工期：0.5 天。

8) Intel 数据集与环境预测说明（低）
   - 现状：`intel_dataset_loader.py` 与 `pytorch_*_env.py` 的实验性使用需简明描述处理流程与作用范围。
   - 影响：场景敏感性与外部数据的可复现度。
   - 修复：补充数据预处理与预测模块的简表与流程说明。
   - 工期：0.5 天。

## 完成度量化估算（当前）
- 方法与实现完备度：80%
- 图表与可视化：65%（图 3 未完成拉低）
- 统计分析与显著性：85%（Welch + Holm-Bonferroni + BH-FDR 已具备）
- 复现实验与打包：70%
- 文稿与章节对齐：60–65%

## 收敛时间线（保守估计，工作日）
- D1：补齐图 3 + 效应量方案改造 + 导出流程校验
- D2：信道模型一致性核对 + 能耗参数与敏感性说明 + 方法接口摘要草稿
- D3：复现清单完善 + 基线参数对齐 + 公平性与敏感性补充
- D4：全文图表一致性核对 + 最终打包 + 交叉校验

## 最小可复现命令草案（待实测校正）
- `python scripts/run_experiments.py` 或 `python scripts/run_aeris_50x200.py`
- `python scripts/run_stats_multitest.py --input results/compare_50x200.json --method holm-bonferroni --bh`
- `python scripts/run_export.py --figset minimal --out results/publication_figures`
- `python scripts/curate_figures.py --src results/publication_figures --dst results/plots_curated`
- `python scripts/validate_figures.py --src results/plots_curated`

## 风险与缓解
- 缺失文件风险：如 `realistic_channel_model.py` 与效应量脚本；若无法补齐，提供替代实现与清晰说明。
- 重现一致性风险：统一随机种子、版本与依赖（参考 `requirements.txt` 与 `scripts/verify_dependencies.py`）。
- 图表风格一致性：统一 `matplotlib` 样式与标签生成逻辑（`plot_paper_figures.py`）。

## 建议的下一步（48 小时内）
- 立即补齐图 3 的 Mermaid 流程并在 `run_export.py` 中纳入生成。
- 确认并修复 `RealisticChannelModel` 文件路径与接口一致性；同步到 AERIS 使用处。
- 在方法章节补充能耗参数表与校准说明，并输出敏感性小图（折线或棒棒糖）。
- 固化导出与统计管线，形成一条可复现命令序列并在 `RUN_GUIDE.md` 记录。

— 结论 —
- 差距约 25–30%，主要集中在图表与文稿对齐以及少量模块一致性与复现固化；按上述计划推进，3–4 个工作日可达到投稿级别。