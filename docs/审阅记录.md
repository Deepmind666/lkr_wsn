# 审阅记录（Round 1：学术规范与版式）

## 发现与行动项
- 引用/参考文献
  - `references/main.bib` 已精简为30篇，字段齐全（作者、卷期页码、DOI）。2025年占9篇，满足要求。
  - 正文交叉引用正常，无缺失键；后续新增文献需保持30篇上限，替换而非累加。
- 版式/排版
  - 仍有 Underfull/Overfull 警告（长段落、表格命令行）。需在最终提交前适度断行或简化长命令。
  - 图表需重绘（Arial/Times、WCAG配色、矢量≥600dpi）。优先：动态走廊、随机掉点、大规模300/500。
- 语言/AI痕迹
  - 部分句子略显口语化，可在后续润色阶段收紧用词、减少“will be filled”等未来式表述。
- 结构
  - 结论与创新点可再强调“纯算法+可重现+MCU友好+真实信道”优势，针对审稿质疑提前回应。

## 下一轮计划（Round 2：方法与图表）
- 重绘关键图（动态走廊、随机掉点、大规模）：统一配色、线型、字体，生成高分辨率矢量，替换 LaTeX 引用。
- 检查表格与长命令，消除主要 Overfull/Underfull。
- 在 Related Work/Discussion 增补 AERIS 相对经典（LEACH/HEED/PEGASIS/TEEN）和当代 (IoT/水下) 的优势说明。

## Round 2 进展（2025-11-23）
- 已在 `for_submission/final_paper.tex` 的 Related Work 中引入全量 30 篇参考文献交叉引用（9 篇 2025 年、2 篇 2024 年、19 篇经典/近期），覆盖安全/信任、能源补给、移动 Sink、LPWAN 协同、LQI/阴影测量、定位与水下路由等方向。
- 现有 30 个 bib 键均被正文引用，符合“总计 30 篇、2025≥8 篇”的要求；如需替换，请按“替换而非累加”原则保持 30 上限。
- 后续待做：图表重绘（WCAG 配色/Arial 或 Times、600dpi 矢量）、版式微调以消除 Under/Overfull、方法论与统计显著性补充、语言润色（减少口语与 AI 痕迹）。

## 本轮执行要点（2025-11-23）
- 修正图路径为相对路径，确保在 `for_submission/` 下直接编译；`latexmk -xelatex` 已生成 `for_submission/final_paper.pdf`。
- 重写复现清单表格（Table~\ref{tab:repro_manifest}），使用 `\resizebox{\textwidth}{!}{...}` 消除 Overfull；现无 Overfull 报警。
- 仍有若干 Underfull 提示（长句、公式行）和 CJK 等宽字体警告（缺少 `\setCJKmonofont`）；后续润色阶段可适度断句或为等宽字体指定 `Microsoft YaHei Mono` 以消除警告。

## 追加进展（2025-11-23）
- 摘要已写入最终数值（Intel PDR 0.389→0.524，能耗仅+0.8 J；50x100 场景 PDR 0.817/36.8 J；覆盖动态/大规模结论），删除“稍后填充”措辞。
- 改写 Introduction 首段分句，减轻长句 Underfull。
- CJK 等宽字体已设定（SimSun），编译无 Overfull，剩余为少量 Underfull 与 hyperref 数学书签提醒。

## 下一步（图表矢量化与版式收尾）
- 图表需重绘（WCAG 配色、Arial/Times、≥600dpi、PDF/SVG 双份）并替换 `results/plots/` 引用：
  - Figure 动态走廊（80 nodes，4 phases，PDR+Energy 双面板，含±1σ阴影）。
  - Figure 移动基站（80 nodes，4 phases，PDR+Energy 双面板，含±1σ阴影）。
  - Figure 随机掉点（120 nodes，3 级 dropout，PDR+Energy 双面板，含±1σ阴影）。
  - Figure 大规模 300/500（PDR+Energy，带 95% CI，配合 Table~\ref{tab:large_scale_summary}）。
  - 方法流程图（CAS/Skeleton/Gateway 信息流，Layered layout，矢量版）。
- 版式/警告收尾：
  - 对含数学符号的书签（表/图 caption 的 $\\times$、$t$-tests 等）使用 `\\texorpdfstring{}{}` 或改写为文本，清理 hyperref 提醒。
  - 长句 Underfull：适度断句，保持 MDPI Sensors 行距；必要时在超长命令/路径前插入换行。

## 图表重绘执行方案（立即可跑）
- 配色/字体：统一 WCAG 可读色（示例：`#0072B2`, `#D55E00`, `#009E73`, `#CC79A7`, `#E69F00`），线宽 1.2、marker size 4、字体 Arial/Times；导出 PDF+SVG，dpi=600。
- 建议命令（仓库根目录）：
  - 动态走廊：`python scripts/plot_paper_figures.py --scenario dynamic_corridor --palette wcag --font Arial --dpi 600 --svg --pdf`
  - 移动 BS：`python scripts/plot_paper_figures.py --scenario moving_bs --palette wcag --font Arial --dpi 600 --svg --pdf`
  - 随机掉点：`python scripts/plot_paper_figures.py --scenario dropout --palette wcag --font Arial --dpi 600 --svg --pdf`
  - 大规模 300/500：`python scripts/plot_paper_figures.py --scenario large_scale --palette wcag --font Arial --dpi 600 --svg --pdf`
  - 方法流程图（如需）：`python scripts/plot_flow_diagram.py --output results/plots/paper_method_flowchart.pdf --svg --font Arial --dpi 600`（或改用 TikZ 版本）
- 输出：覆盖 `results/plots/` 下同名 PDF/SVG，LaTeX 引用已指向该目录，无需改路径。

## 本轮进展（2025-11-23 夜）
- 已修改生成脚本，动态走廊/移动 BS/随机掉点/大规模 300-500 均自动导出 SVG+PDF；已重新生成并覆盖 `results/plots/`。
- 重新编译 LaTeX（无 Overfull/hyperref 警告），PDF 最新位于 `for_submission/final_paper.pdf`。
