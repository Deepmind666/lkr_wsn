# 1月27日项目说明（交接用）

> 日期：2026-01-27  
> 适用对象：新接手同事 / 外部审查同事  
> 范围：算法、实验、论文与结果产物

---

## 1. 项目总体目标
- 目标期刊：**MDPI Sensors（Q2）**
- 目标：保证**算法创新性**与**实验严谨性**达标，同时明确算法边界与失败场景。

---

## 2. 当前真实进度（务必说明）
- **过夜实验未真实运行成功**（2026-01-27 00:51 左右）：
  - `results/overnight_8h_log.txt` 显示因 `ImportError` 失败
  - 失败原因：旧版脚本 `scripts/run_overnight_8h.py` 依赖了已废弃类名 (`AERISProtocol`, `AERISConfig`)
  - **无正在运行的实验进程**（本机仅见 VSCode 自动格式化 Python 进程）
- **已修复**：我已重写 `scripts/run_overnight_8h.py` 为“调用现有脚本的调度器”，避免 API 变更导致崩溃。
- **已验证存在的权威数据源**：
  - `results/large_scale_scalability_verified.json` ✅

---

## 3. 目录结构与关键文件（新同事必读）

### 3.1 核心代码目录
- `src/aeris_protocol.py` — AERIS 主协议实现（核心入口）
- `src/benchmark_protocols.py` — 标准基线协议（LEACH/PEGASIS/HEED/TEEN）
- `src/improved_energy_model.py` — CC2420 能耗模型
- `src/realistic_channel_model.py` — Log-Normal Shadowing 信道模型
- `src/gateway_selector.py` — 网关选择器

### 3.2 实验脚本目录（主力）
- `scripts/run_large_scale_scalability.py` — 大规模扩展性（支持 --replicates / --workers）
- `scripts/run_dynamic_corridor_compare.py` — 动态走廊对比
- `scripts/run_dynamic_moving_bs_compare.py` — 移动基站场景
- `scripts/run_dynamic_dropout_compare.py` — 随机失效场景
- `scripts/run_intel_ablation_parallel.py` — Intel 消融（并行，支持 repeats/workers）
- `scripts/run_intel_sensitivity_parallel.py` — Intel 敏感性（并行）
- `scripts/run_overnight_8h.py` — **过夜实验调度器（已修复）**

### 3.3 论文目录
- `for_submission/` — 论文提交相关材料
  - `aeris_paper_final.tex / pdf`（若有）
  - `figures/`（当前投文所有图表）

### 3.4 结果输出目录
- `results/` — 所有输出 JSON + 日志
- `results/run_logs/` — 子实验日志

### 3.5 评审/计划文档
- `docs/` — 改进计划、审查意见、对接文档

---

## 4. 当前已知风险与漏洞（必须继承）
1. **过夜脚本旧版已崩溃**：
   - 原因：类名/接口变更，导致 ImportError
   - 已修复脚本版本：`scripts/run_overnight_8h.py`
2. **SOTA 对比脚本仍存在不稳定风险**：
   - `run_sota_comparison_v2.py` 只跑基线，依赖已有 AERIS 结果
   - 若 AERIS 结果缺失，会出现统计对比缺口
3. **动态实验脚本不支持多核并行**：
   - 需通过增加 replicates 提升统计可信度（CPU 利用率较低）

---

## 5. 实验执行说明（新版调度器）
已修复脚本：
```
python scripts/run_overnight_8h.py --workers 22 --intel-repeats 50 --dynamic-reps 30 --scale-reps 30
```
- 调度器会顺序调用：
  - Intel 消融 / 敏感性
  - 动态走廊 / 移动 BS / 随机失效
  - 大规模扩展性
  - （可选）SOTA 基线对比

输出：
- `results/overnight_8h_complete_YYYYMMDD_HHMMSS.json`
- `results/run_logs/*.log`

---

## 6. 建议的交接优先级
**P0（必须先做）**
- 复核 `large_scale_scalability_verified.json` 与现有论文表格一致性
- 使用新调度器重新完成过夜实验

**P1（接续优化）**
- 强化 AERIS v2（AMG/EAS/CR/PLQ/OCL）实验效果
- 对比图表修复（遮挡、图例位置、统计说明）

---

## 7. 结论与建议
- 当前代码结构可继续推进，但**实验脚本稳定性**是近期最大问题。
- 新同事接手前必须先了解：
  - **哪些结果是真实运行得到**
  - **哪些结果仅来自旧脚本或不完整统计**

---

> 如需进一步对接，请优先看 `docs/2026-01-27_handover.md` 与本文件。
